Texture2D mappedSRV;

SamplerState ss;

// It's also convenient to set SamplerState in ps.
//SamplerState ss
//{
//	Filter = ANISOTROPIC;
//	MaxAnisotropy = 4;
//	AddressU = WRAP;
//	AddressV = WRAP;
//};

struct PixelIn
{
	float4 pos : SV_POSITION;
	float2 tex : TEXCOORD0;	
};

float4 PS(PixelIn pin) : SV_TARGET
{
	return mappedSRV.Sample(ss, pin.tex);
}